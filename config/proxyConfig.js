// 不执行代理
module.exports = {
	// proxy: {
	// 	'/api': {
	// 		target: 'http://127.0.0.1:9000', // 代理本地9000端口，secure设置为false
	// 		// target: 'https://notes-api.tanglihe.cn',// 代理到线上的域名，https 协议，secure设置为true
	// 		secure: false,  // 如果是https接口，需要配置这个参数
	// 		changeOrigin: true, // 是否跨域
	// 		'^/api': '/'
	// 	}
	// }
}